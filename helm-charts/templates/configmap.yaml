apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quiz-app.fullname" . }}-questions
  labels:
    {{- include "quiz-app.labels" . | nindent 4 }}
data:
  {{ .Values.configmap.questionsFile }}: |
{{ .Files.Get (printf "questions/%s" .Values.configmap.questionsFile) | indent 4 }}
